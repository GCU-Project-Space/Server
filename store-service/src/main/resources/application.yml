spring:

  application:
    name: STORE-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/store_service?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 411l1580!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  doc:
    api-docs:
      path: /api-docs

user:
  service:
    url: http://localhost:9999  # 또는 mock 주소

server:
  error:
    include-exception: false
    include-stacktrace: never

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://3.36.183.156:8761/eureka
